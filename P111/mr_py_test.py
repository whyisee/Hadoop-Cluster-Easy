#!/usr/bin/env python

import re
import sys

for line in sys.stdin:
    #max_value = line.split(chr(1))[1]
    #max_key = line.split(chr(1))[0]
    
    if (line == "1"):
        print (line)
    else:
        print (11)
